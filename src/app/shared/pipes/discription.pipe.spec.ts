import { DiscriptionPipe } from './discription.pipe';

describe('DiscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
